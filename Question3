A branch is a parallel 'version' of a repo, where you can experiment with different changes to the files without altering the master branch. If necessary, you can then merge the changes into the master branch.
